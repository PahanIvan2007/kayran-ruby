<div class="page-header">
  <div class="container">
    <h1>Редактировать турнир</h1>
    <p>Изменение параметров турнира</p>
  </div>
</div>
<div class="container">
  <div class="card border-0 shadow-sm" style="border-radius: 12px; max-width: 600px;">
    <div class="card-body p-4">
      <%= form_with(model: @tournament, class: "row g-4") do |f| %>
        <% if @tournament.errors.any? %>
          <div class="col-12">
            <div class="alert alert-danger d-flex align-items-center gap-2">
              <i class="bi bi-exclamation-triangle-fill flex-shrink-0"></i>
              <div>
                <strong><%= pluralize(@tournament.errors.count, "ошибка") %>:</strong>
                <ul class="mb-0 mt-1"><% @tournament.errors.each do |err| %><li><%= err.full_message %></li><% end %></ul>
              </div>
            </div>
          </div>
        <% end %>
        <div class="col-12"><%= f.label :title, class: "form-label fw-medium" %><%= f.text_field :title, class: "form-control", required: true %></div>
        <div class="col-12 col-md-6"><%= f.label :event_id, class: "form-label fw-medium" %><%= f.collection_select :event_id, Event.upcoming, :id, :title, { include_blank: "—" }, class: "form-select" %></div>
        <div class="col-12 col-md-6"><%= f.label :format, class: "form-label fw-medium" %><%= f.select :format, Tournament.formats.keys.map { |k| [t("activerecord.attributes.tournament.formats.#{k}"), k] }, {}, class: "form-select" %></div>
        <div class="col-12 col-md-6"><%= f.label :status, class: "form-label fw-medium" %><%= f.select :status, Tournament.statuses.keys.map { |k| [t("activerecord.attributes.tournament.statuses.#{k}"), k] }, {}, class: "form-select" %></div>
        <div class="col-12 d-flex gap-2 pt-3 border-top"><%= f.submit class: "btn btn-primary btn-modern px-4" %><%= link_to "Отмена", tournaments_path, class: "btn btn-outline-secondary btn-modern px-4" %></div>
      <% end %>
    </div>
  </div>
</div>
